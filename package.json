{
  "name": "@daliusd/lang-lsp",
  "version": "0.3.0",
  "description": "Language Server Protocol server for language string collection with i18next namespace support",
  "main": "dist/server.js",
  "bin": {
    "lang-lsp": "./bin/lang-lsp"
  },
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "prepublish": "npm run build",
    "test": "echo 'Open test files in your editor to test LSP functionality. See README for details.' && exit 0",
    "test:open": "echo 'Opening test files...' && cd test && (nvim fixtures/sample.ts || code fixtures/sample.ts || echo 'Please open test/fixtures/*.ts files in your editor')"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/daliusd/lang-lsp.git"
  },
  "author": "Dalius Dobravolskas",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "lsp",
    "language-server",
    "i18n",
    "localization"
  ],
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "nanolru": "^1.0.0",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "files": [
    "bin",
    "dist",
    "LICENSE",
    "README.md"
  ]
}
